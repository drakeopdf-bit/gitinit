document.getElementById('propose-btn').addEventListener('click', function() {
    const reveal = document.getElementById('proposal-reveal');
    reveal.style.display = 'block';
    // Optional: Add confetti effect (requires a library like confetti.js)
    // confetti();
});